import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

// The language reference is a single document; its headings drive the on-page
// table of contents, so the sidebar has nothing to nest.
const sidebars: SidebarsConfig = {
  relqlSidebar: ['index'],
};

export default sidebars;
